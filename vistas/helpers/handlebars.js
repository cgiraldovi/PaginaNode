module.exports = {
    calculadora: (n1, n2) => {
        let resultado = parseInt(n1) + parseInt(n2);
        return `El resultado de ${n1} + ${n2} es ${resultado}`;
    },
    f1: ()=>{
        return `hola mundo`
    }
}